"""Xero connector stub — replaced by CSV ingestion in PHLedger."""


class XeroConnector:
    """Stub. Xero integration removed. Use CSV import instead."""

    def __init__(self):
        raise NotImplementedError(
            "Xero integration is not supported. "
            "Export CSV from your bank and use /upload endpoint."
        )
